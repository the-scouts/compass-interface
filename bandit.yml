# bandit configuration

# skip checking usages of assertions in test files
assert_used:
  skips: ['*/*_test.py', '*/test_*.py']

skips: [
    'B410', # import lxml. defusedxml.lxml is deprecated. TODO Maybe disable entity expansion with `resolve_entities=False`?
]