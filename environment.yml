name: compass-interface
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.9

  # Compass-Interface Core
  - compass-interface-core>=0.3.0

  # Transforming
#  - numba>0.52  # Numba==0.52 does not support python 3.9!
  - pandas
  - pyarrow

  # API
  - fastapi
  - conda-forge::python-jose  # JSON Web Tokens (Must use CF build for resolver to succeed)
  - aioredis  # (Async) Redis
  - python-multipart  # Form Data (OAuth2)

  # Deployment
  - uvicorn
#  - gunicorn  # *NIX only, no windows support

  # Testing:
#  - pytest
#  - hypothesis
#  - pytest-cov

  # Development requirements:
#  - pre-commit
  - black
  - isort
  - ipython
#  - dtale
